import { Default } from 'components/layouts/Default';
import { Auctions } from 'components/templates/auctions';

const AuctionsPage = () => {
  return (
    <Default pageName="Auctions">
      <Auctions />
    </Default>
  );
};

export default AuctionsPage;
